# @constructive-io/brand-logo

The Constructive mark — a "C" built from six unit cubes — as grid, voxel model, SVG and OBJ.

```ts
import { MARK, MARK_GRID, renderMark, exportMark } from '@constructive-io/brand-logo';

renderMark({ colors: colorways.dark }).svg;
exportMark().obj;
```
